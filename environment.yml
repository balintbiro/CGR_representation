name: chaos_rep
channels:
  - defaults
dependencies:
  - python=3.10
  - scikit-learn
  - pytorch
  - click
  - requests
  - pandas
  - biopython
  - torchvision
  - huggingface_hub
  - pip

  - pip:
    - skorch
prefix: /home/user/miniconda3/envs/chaos_rep
